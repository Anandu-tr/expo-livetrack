{
  "name": "expo-livetrack-test-server",
  "version": "0.0.0",
  "private": true,
  "description": "Dummy in-package ingestion server for isolated validation of the /locations/batch upload contract. Dev-only; not published.",
  "scripts": {
    "start": "ts-node server.ts",
    "test": "jest"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/jest": "^29.5.12",
    "@types/node": "^20.14.0",
    "@types/supertest": "^6.0.2",
    "jest": "^29.7.0",
    "supertest": "^7.0.0",
    "ts-jest": "^29.2.5",
    "ts-node": "^10.9.2",
    "typescript": "^5.5.4"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node"
  }
}
